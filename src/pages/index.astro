---
/**
 * Главная `/`. Hero → один сильный блок услуг (карточки с фото) → о компании →
 * преимущества → география (8 городов) → конверсия. Без дублирующих блоков услуг
 * (см. правки → пункты 2, 4). Schema: Organization (сайтово). Без хлебных крошек.
 */
import Base from '../layouts/Base.astro';
import Hero from '../components/Hero.astro';
import ServiceCard from '../components/ServiceCard.astro';
import CityCard from '../components/CityCard.astro';
import AnswerFirst from '../components/AnswerFirst.astro';
import CtaSection from '../components/CtaSection.astro';
import Media from '../components/Media.astro';
import Icon from '../components/icons/Icon.astro';
import type { IconName } from '../components/icons/paths';
import { services } from '../data/services';
import { cities } from '../data/cities';

const title = 'Spectrum Guard — охрана и личная безопасность в 8 городах России';
const description =
  'Spectrum Guard — охранное агентство: личная охрана, защита при угрозах, охрана при ' +
  'переезде, помощь при конфликте с соседями и охрана мероприятий. Работаем в Москве, ' +
  'Санкт-Петербурге, Екатеринбурге, Новосибирске, Уфе, Челябинске, Перми и Тюмени. ' +
  'Оставьте заявку или напишите в Telegram.';

const why = [
  {
    icon: 'guard' as const,
    title: 'В правовом поле',
    text: 'Обеспечиваем безопасность и фиксируем факты, но не давим на третьих лиц. Защита, а не самосуд.',
  },
  {
    icon: 'threat' as const,
    title: 'Конфиденциально',
    text: 'Детали задачи и данные клиента не разглашаем. Работаем дискретно — без лишнего внимания.',
  },
  {
    icon: 'pin' as const,
    title: 'Выезд по 8 городам',
    text: 'Москва, Санкт-Петербург, Екатеринбург, Новосибирск, Уфа, Челябинск, Пермь, Тюмень. Маршрут — заранее.',
  },
  {
    icon: 'clock' as const,
    title: 'Заявки 24/7',
    text: 'Перезвоним или ответим в Telegram. Подберём формат под задачу — от разовой смены до постоянного режима.',
  },
];
---

<Base {title} {description} path="/">
  <Hero />

  {/* Один сильный блок услуг — карточки с реальными кадрами */}
  <section class="section container" aria-labelledby="services-heading">
    <p class="eyebrow">Что мы делаем</p>
    <h2 id="services-heading" class="home-h2">Пять направлений</h2>
    <ul class="services-grid">
      {
        services.map((s, i) => (
          <li>
            <ServiceCard
              label={s.intentLabel}
              sub={s.tagline}
              href={`/uslugi/${s.slug}/`}
              icon={s.icon as IconName}
              image={s.cardImage}
              index={i + 1}
            />
          </li>
        ))
      }
    </ul>
  </section>

  {/* О компании — с кадром */}
  <section class="section container" aria-labelledby="about-heading">
    <div class="about-home">
      <div class="about-home__text">
        <p class="eyebrow">О компании</p>
        <h2 id="about-heading" class="home-h2">Охрана под конкретную задачу</h2>
        <AnswerFirst
          text="Spectrum Guard — охранное агентство для частных клиентов и бизнеса. Помогаем там, где нужна безопасность человека: личная охрана и сопровождение, защита при угрозах, спокойный переезд при конфликте, помощь с соседями и охрана мероприятий. Работаем в восьми городах России выездным форматом, в правовом поле и конфиденциально."
        />
        <p class="about-home__more">
          Подбираем формат под ситуацию — от разовой смены до постоянного режима. Сначала
          короткий разговор: что происходит, какие риски, что нужно. Затем согласуем план,
          состав и стоимость — без скрытых доплат.
        </p>
        <a href="/o-kompanii/" class="about-home__link mono">
          Подробнее о компании <Icon name="arrow" size={16} />
        </a>
      </div>
      <figure class="about-home__media reveal">
        <Media
          src="neutral/neutral-penthouse-interior-01.webp"
          alt="Spectrum Guard — сопровождение и личная безопасность"
          sizes="(max-width: 900px) 100vw, 45vw"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  {/* Преимущества */}
  <section class="section container" aria-labelledby="why-heading">
    <p class="eyebrow">Почему Spectrum Guard</p>
    <h2 id="why-heading" class="home-h2">Спокойно, по делу, без пафоса</h2>
    <ul class="why">
      {
        why.map((w) => (
          <li class="why__item perimeter reveal">
            <Icon name={w.icon} size={28} class="why__icon" />
            <h3 class="why__title">{w.title}</h3>
            <p class="why__text">{w.text}</p>
          </li>
        ))
      }
    </ul>
  </section>

  {/* География — 8 городов с кадрами, выбор города */}
  <section class="section container" aria-labelledby="geo-heading">
    <div class="geo-head">
      <div>
        <p class="eyebrow">География</p>
        <h2 id="geo-heading" class="home-h2">8 городов России</h2>
      </div>
      <a href="/goroda/" class="geo-head__all mono">Все города <Icon name="arrow" size={16} /></a>
    </div>
    <ul class="geo-grid">
      {
        cities.map((c) => (
          <li><CityCard city={c} /></li>
        ))
      }
    </ul>
  </section>

  <CtaSection source="Главная страница" />
</Base>

<style>
  .home-h2 {
    max-width: 24ch;
    margin: var(--space-2) 0 var(--space-6);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: var(--col-gap);
  }
  @media (max-width: 1100px) {
    .services-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  @media (max-width: 720px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 460px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }

  .about-home {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: center;
  }
  .about-home__more {
    color: var(--text-300);
    max-width: 60ch;
    margin-top: var(--space-6);
  }
  .about-home__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    margin-top: var(--space-6);
    color: var(--accent);
  }
  .about-home__media {
    aspect-ratio: 4 / 3;
    overflow: hidden;
    border-radius: var(--radius);
    border: 1px solid var(--line);
  }
  .about-home__media :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  @media (max-width: 900px) {
    .about-home {
      grid-template-columns: 1fr;
    }
    .about-home__media {
      order: -1;
    }
  }

  .why {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--col-gap);
    margin-top: var(--space-6);
  }
  .why__item {
    padding: var(--space-6);
    border: 1px solid var(--line);
    background: var(--bg-800);
  }
  .why__icon {
    color: var(--accent);
    margin-bottom: var(--space-4);
  }
  .why__title {
    margin-bottom: var(--space-2);
  }
  .why__text {
    color: var(--text-300);
    font-size: var(--fs-small);
  }
  @media (max-width: 900px) {
    .why {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 480px) {
    .why {
      grid-template-columns: 1fr;
    }
  }

  .geo-head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }
  .geo-head .home-h2 {
    margin-bottom: 0;
  }
  .geo-head__all {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    color: var(--accent);
    white-space: nowrap;
    padding-bottom: var(--space-2);
  }
  .geo-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--col-gap);
  }
  @media (max-width: 1000px) {
    .geo-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 520px) {
    .geo-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
